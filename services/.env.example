PORT=3000

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=your_database_name

JWT_SECRET=your_secret_key

# Run this in your terminal, this will generate your JWT_SECRET
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"